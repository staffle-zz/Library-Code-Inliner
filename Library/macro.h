#define fr(i, a, b) for (int i = (a); i != b; i += 1 - 2 * ((a) > (b)))
#define fa(i, x) for (auto &i : x)
#define sz(x) ((int)x.size())
#define len(x) ((int)x.length())
#define F first
#define S second
#define all(x) begin(x), end(x)